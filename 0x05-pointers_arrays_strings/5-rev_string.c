#include "main.h"
/**
 * rev_string - function that reverses a string
 * @s: input
 */
void rev_string(char *s)
{
	int i, n;
	rev = 0;

	for (i = 0; s[i] != '\0'; i++)
	{
		rev++;
	}
	for (n = (rev - 1); n >= 0; n--)
	{
		s[i];
	}
}
